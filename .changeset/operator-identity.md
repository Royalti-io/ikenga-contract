---
'@ikenga/contract': minor
---

Add `OperatorIdentity` hostContext extension (`operator` field on `IkengaHostContextExtensions`) — optional; absence means unknown operator and consumers must fail safe (e.g. `ux_mode: 'confirm'`).
